<% if @article.errors.any? %>
  <h2 style="color:red;">following errors prevented the article from created</h2>
  <ul>
    <% @article.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>